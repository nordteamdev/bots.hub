<li class="list-group-item">
    <?php 
    echo '<b>' . $wo['field']['name'] . ':</b> ';
    if ($wo['field']['select_type'] == 'yes') {
      	$options = @explode(',', $wo['field']['type']); 
      	echo $options[$wo['user_profile']['fields'][$wo['field']['fid']] - 1];
    } else {
      	echo $wo['user_profile']['fields'][$wo['field']['fid']];
    }
    ?>
</li>